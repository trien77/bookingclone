

export default{
    plugins: {
      'postcss-import': {}, // Helps to @import CSS files
      'autoprefixer': {},   // Adds vendor prefixes to CSS rules
      'cssnano': {},        // Minifies CSS
      'tailwindcss': {},
      'autoprefixer': {},
      // You can add more plugins as needed
    }
  }